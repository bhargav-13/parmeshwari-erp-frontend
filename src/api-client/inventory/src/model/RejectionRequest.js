/**
 * Inventory API
 * OpenAPI for inventory management
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReturnType from './ReturnType';

/**
 * The RejectionRequest model module.
 * @module model/RejectionRequest
 * @version 1.0.0
 */
class RejectionRequest {
    /**
     * Constructs a new <code>RejectionRequest</code>.
     * @alias module:model/RejectionRequest
     * @param partyId {Number} Party ID from purchase_party master
     * @param date {Date} 
     * @param weight {Number} Weight of the rejected item in kg
     * @param returnType {module:model/ReturnType} 
     */
    constructor(partyId, date, weight, returnType) { 
        
        RejectionRequest.initialize(this, partyId, date, weight, returnType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, partyId, date, weight, returnType) { 
        obj['partyId'] = partyId;
        obj['date'] = date;
        obj['weight'] = weight;
        obj['returnType'] = returnType;
    }

    /**
     * Constructs a <code>RejectionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RejectionRequest} obj Optional instance to populate.
     * @return {module:model/RejectionRequest} The populated <code>RejectionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RejectionRequest();

            if (data.hasOwnProperty('partyId')) {
                obj['partyId'] = ApiClient.convertToType(data['partyId'], 'Number');
            }
            if (data.hasOwnProperty('date')) {
                obj['date'] = ApiClient.convertToType(data['date'], 'Date');
            }
            if (data.hasOwnProperty('weight')) {
                obj['weight'] = ApiClient.convertToType(data['weight'], 'Number');
            }
            if (data.hasOwnProperty('returnType')) {
                obj['returnType'] = ReturnType.constructFromObject(data['returnType']);
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('stockItemId')) {
                obj['stockItemId'] = ApiClient.convertToType(data['stockItemId'], 'Number');
            }
            if (data.hasOwnProperty('returnQty')) {
                obj['returnQty'] = ApiClient.convertToType(data['returnQty'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RejectionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RejectionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RejectionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

RejectionRequest.RequiredProperties = ["partyId", "date", "weight", "returnType"];

/**
 * Party ID from purchase_party master
 * @member {Number} partyId
 */
RejectionRequest.prototype['partyId'] = undefined;

/**
 * @member {Date} date
 */
RejectionRequest.prototype['date'] = undefined;

/**
 * Weight of the rejected item in kg
 * @member {Number} weight
 */
RejectionRequest.prototype['weight'] = undefined;

/**
 * @member {module:model/ReturnType} returnType
 */
RejectionRequest.prototype['returnType'] = undefined;

/**
 * Cash amount returned — required when returnType is CASH
 * @member {Number} amount
 */
RejectionRequest.prototype['amount'] = undefined;

/**
 * Stock item ID from inventory — required when returnType is MAAL
 * @member {Number} stockItemId
 */
RejectionRequest.prototype['stockItemId'] = undefined;

/**
 * Quantity (kg) to deduct from stock item — required when returnType is MAAL
 * @member {Number} returnQty
 */
RejectionRequest.prototype['returnQty'] = undefined;






export default RejectionRequest;

